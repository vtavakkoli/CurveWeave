{
  "name": "curveweave",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A privacy-first, browser-native SVG editor, inspector, repair and optimization toolkit.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/svg-utils.js && node --check src/enhancements.js && node --check src/selection-utils.js && node --check src/advanced-selection.js && node --check src/path-geometry.js && node --check src/boolean-utils.js && node --check src/pro-vector.js && node --check sw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
